# Server
PORT=5000
NODE_ENV=development

# CORS
CORS_ORIGIN=*

# Database (Prisma)
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/zomaal_backend"

# JWT (access token + opaque refresh tokens stored in DB)
JWT_ACCESS_SECRET=
JWT_ACCESS_EXPIRES_MIN=15
JWT_REFRESH_EXPIRES_DAYS=7
